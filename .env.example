# Convex Configuration
CONVEX_DEPLOYMENT=
PUBLIC_CONVEX_URL=

# Google Calendar API Configuration
# Get these from https://console.cloud.google.com/
PUBLIC_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
PUBLIC_GOOGLE_API_KEY=your-api-key

# For server-side OAuth (optional - not currently used)
GOOGLE_CLIENT_SECRET=your-client-secret

# Instructions:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable the Google Calendar API
# 4. Create credentials:
#    - For API Key: Create an API key and restrict it to your domain
#    - For OAuth 2.0: Create OAuth 2.0 Client ID
#      - Application type: Web application
#      - Authorized JavaScript origins: http://localhost:5173 (and your production domain)
#      - Authorized redirect URIs: http://localhost:5173 (and your production domain)
# 5. Copy the Client ID and API Key here